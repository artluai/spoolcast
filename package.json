{
  "name": "session-to-video",
  "version": "1.0.0",
  "description": "Workflow and Remotion scaffold for turning sessions into videos",
  "repository": {},
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@remotion/cli": "4.0.447",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "remotion": "4.0.447",
    "@remotion/tailwind-v4": "4.0.447",
    "tailwindcss": "4.0.0"
  },
  "devDependencies": {
    "@remotion/eslint-config-flat": "4.0.447",
    "@types/react": "19.2.7",
    "@types/web": "0.0.166",
    "eslint": "9.19.0",
    "prettier": "3.8.1",
    "typescript": "5.9.3"
  },
  "scripts": {
    "dev": "remotion studio",
    "build": "remotion bundle",
    "upgrade": "remotion upgrade",
    "lint": "eslint src && tsc",
    "voiceover:generate": "echo \"Add a project-specific voiceover script before using this command.\" && exit 1",
    "images:generate:kie": "echo \"Add a project-specific image-generation script before using this command.\" && exit 1",
    "shot-review:build": "echo \"Add a project-specific review-board build script before using this command.\" && exit 1",
    "shot-review:fetch-assets": "echo \"Add a project-specific asset-fetch script before using this command.\" && exit 1"
  },
  "sideEffects": [
    "*.css"
  ]
}
